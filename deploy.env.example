# Параметры публичного сервера (справочно; в compose не подставляются автоматически)
PUBLIC_HOST=81.26.177.173
PUBLIC_URL=http://81.26.177.173

# Команда деплоя:
# docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build
