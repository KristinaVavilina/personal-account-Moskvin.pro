# Скопируйте на сервер в deploy.env (не коммитьте с реальными секретами).
# Только для документации и скриптов — React/nginx API не используют эти переменные.

PUBLIC_HOST=158.160.233.204
PUBLIC_URL=http://158.160.233.204

# Деплой:
# docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build
