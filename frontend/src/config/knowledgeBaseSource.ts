/**
 * Переключение источника данных базы знаний.
 * `true` — локальные моки в формате ответов API.
 * `false` — запросы к бэкенду через прокси `/api/*`.
 */
export const USE_KNOWLEDGE_BASE_MOCK = true;
